sparkta {

  api {
    host = 0.0.0.0
    port = 9090
  }

  spark {
    spark.master = "local[2]"
    spark.cores.max = 2
    spark.executor.memory = 1024m
    spark.app.name = SPARKTA-context
  }

}